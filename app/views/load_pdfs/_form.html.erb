<%= simple_form_for [@load, @load_pdf] do |f| %>

  <div class="form-inputs">
    <%= f.input :html %>
    <%= f.input :load_id %> 
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
